!!!
%html
  %head
    %meta{charset: 'utf-8'}
    %meta{name: :viewport, content: 'width=device-width, initial-scale=1 shrink-to-fit=no'}
    = webpack_head
    %title= current_page.data.title || 'Wobsite'
  %body
    = yield
    = webpack_body
